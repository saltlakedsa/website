<!DOCTYPE html>
<html lang="en">

  <head>

	<% include ../partials/head %>

  </head>

  <body>

    <!-- Navigation -->
    
	<% include ../partials/nav %>

    <!-- Page Content -->
    <div id="page-top">
	<section id="store">
		<div class="container">
			<div class="row">
				<div class="col-lg-12 text-center">
					<h1 class="section-heading pb-5">Salt Lake DSA Official T-shirt</h1>
				</div>
				
				<!--<% if (locals.content) { %>
					<span class="tb-font-xl">
						<%= content.length %>
					</span>
					<span> </span>
					<% if (content.length > 1) { %>
						<span>
							items for sale
						</span>
					<% } else { %>
						<span>
							item for sale
						</span>
					<% } %><% } %>-->
			</div>
			<div class="row">
					<div class="col-sm-6">
						<% content.forEach(function(item) { %>
						<img src="<%= item.image %>" style="width:100%;"></img>
					
						
						
					</div>
					<div class="col-sm-6">			
						<h3 style="text-transform:uppercase;"><%= item.title %></h3> 
						
						<p class="text-muted"><%= item.description %></p>
						
						<p class="text-muted"><strong>$ <%= (item.price / 100).toFixed(2) %></strong></p>
						<a class="btn btn-primary btn-xl js-scroll-trigger" href="/shop/addtocart/<%= item._id %>">Purchase</a>
						<% }); %>
					</div>	
			</div>
		
		</div>
    </section>
    
	<% include ../partials/socialmedia %>
	  
    </div>
    <!-- /.container -->

    <% include ../partials/footer %>

  </body>

</html>
