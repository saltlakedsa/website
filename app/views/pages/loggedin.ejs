


<!DOCTYPE html>
<html lang="en">

	<head>
	<% include ../partials/head %>
	</head>

	<body>

		<!-- Navigation -->
		<% include ../partials/nav %>
		<!-- Page Content -->
		
			<div class="container" id="page-top">
			<section>
				
			Logged in as
				
			<p>	
			<% var tmp = "not logged in";
			if (user) { tmp = user.username;} %>
			<%= tmp %>
			
				
			<div>
			<a class="btn btn-primary" href="/logout">LOG OUT</a>
			
			<% var failed = (parameters.authfail) ? "yes" : "no" %>

			<%= failed %>
			
			</section>
			</div>
			<% include ../partials/socialmedia %>
			
		
		<!-- /.container -->
		<% include ../partials/footer %>
	</body>

</html>
